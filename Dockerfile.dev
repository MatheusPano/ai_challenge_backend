FROM node:20-alpine

WORKDIR /app

# Install deps first for better caching
COPY package.json package-lock.json* ./
RUN npm install

# Source is bind-mounted in compose; this COPY is a fallback for build-only.
COPY . .

EXPOSE 4000

CMD ["npm", "run", "start:dev"]
